apply plugin: 'java'

defaultTasks 'classes'

repositories {
    mavenRepo urls: 'http://www.aQute.biz/repo'
    mavenRepo urls: 'http://repository.jboss.org/maven2'
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'org.jboss.maven.plugins:maven-jdocbook-plugin:2.2.3'
}